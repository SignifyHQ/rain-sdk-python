#!/usr/bin/env bash

set -eux
rm -rf dist
mkdir -p dist
uv build

# Token is optional: CI authenticates via OIDC trusted publishing.
if [[ -n "${PYPI_TOKEN:-}" ]]; then
  uv publish --token="$PYPI_TOKEN"
else
  uv publish
fi
